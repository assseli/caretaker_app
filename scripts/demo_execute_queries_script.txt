############################################################
# Demo: Using the execute_queries.py Script
#
# Introduction:
# The `execute_queries.py` script is designed to help you interactively execute SQL queries against your project's database. It can be used to create tables, insert data, and run SELECT statements to view or manage your data. This is especially useful for testing, debugging, or manually managing your database during development.
#
# Demo Steps:
# 1. Open your MySQL terminal or client.
# 2. Select the target database using the `USE` statement.
# 3. Run a SQL query to fetch all users from the `app_user` table.
# 4. View the results in your terminal.
#
# The following steps demonstrate how to fetch all users from the database:
############################################################

# 1. Open MySQL Terminal
#    (e.g., run: mysql -u AsselDerbisova -p)
#     password mySql2@24

# 2. Select the Database
USE caregiver_db;

# 3. Fetch All Users
SELECT * FROM app_user;

# 4. View Results
#    The output will display all rows and columns from the app_user table.
